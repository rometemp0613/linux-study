# 2026-02-28 학습 일지

## 복습 퀴즈 결과
- 5문제 중 3정답, 1부분정답, 1오답
- scp 목적지 필수 → 3-mastered 승급
- fstab 6개 필드 → 아직 2-practicing 유지
- ssh -L 포트포워딩 형식 (localhost 빠뜨림) → 부분 정답

## Ch.20: Searching for Files 학습
- locate: DB 기반 빠른 검색, plocate 설치 (Ubuntu)
- find: 이름/타입/크기/시간/권한 조건으로 정밀 검색
- -exec {} \; 패턴, -delete, -ls 동작
- xargs: 파이프로 인자 전달, -print0 | xargs -0 안전 처리
- 실습: find-practice 디렉토리에서 전체 실습 완료

## 실수에서 배운 것
- 중괄호 확장에 공백 넣으면 안 됨: `{a, b}` ❌ → `{a,b}` ✅
- `-exec`는 반드시 `{} \;`로 끝내야 함
- `| wc -l`(개수)과 `xargs wc -l`(내용 줄 수) 구분

## 다음 학습
- Ch.21: Archiving and Backup (tar, gzip, zip, rsync)
